# cluster settings:
executor: slurm
cores: 42
jobs: 5
use-singularity: true # activate singularity for container
singularity-args: "--no-home --home /scratch/midway3/aszatrowski --bind /project/lbarreiro,/scratch/midway3/aszatrowski" # give container access to all of lbarreiro & personal scratch
default-resources:
  slurm_account: pi-lbarreiro # bill Luis
  slurm_partition: lbarreiro # don't bill Luis
